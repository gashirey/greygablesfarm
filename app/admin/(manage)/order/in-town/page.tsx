import { SsInTownPickupManager } from "@/components/admin/SsInTownPickupManager";

export const metadata = {
  title: "In Town pickup",
};

export default function AdminInTownPickupPage() {
  return <SsInTownPickupManager />;
}
